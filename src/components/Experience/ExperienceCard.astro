---
import CardLink from "../Utils/Card/CardLink.astro";
import TextContent from "../Utils/Font/TextContent";
import type { Experience } from "../../data/experiences";

export interface Props extends Pick<
  Experience,
  | "title"
  | "provider"
  | "briefDescription"
  | "tags"
  | "logo"
  | "startDate"
  | "endDate"
  | "type"
> {
  compact?: boolean;
}

const {
  title,
  provider,
  briefDescription,
  tags,
  logo,
  startDate,
  endDate,
  type,
  compact = false,
} = Astro.props;

const visibleTags = compact ? tags.slice(0, 4) : tags;
const subtitle = provider
  ? `${provider} / ${startDate} - ${endDate ?? "Actualidad"}`
  : `${startDate} - ${endDate ?? "Actualidad"}`;
const experienceLink = `/experiencia/${encodeURIComponent(title.replace(/\s+/g, "-"))}`;
---

<CardLink link={experienceLink} compact={compact}>
  {
    logo ? (
      <div class="sm:col-span-10 sm:mt-4 sm:mb-2 md:col-span-3 flex items-center justify-center">
        <img
          src={logo}
          alt={title}
          class="object-contain sm:max-h-14 md:max-h-16 md:max-w-[45%] h-auto"
        />
      </div>
    ) : (
      <div class="hidden md:block md:col-span-3" />
    )
  }
  <div
    class="sm:px-3 sm:py-2 md:py-3 sm:border-t-2 md:border-l-2 sm:pt-3 sm:border-slate-200 md:px-4 sm:col-span-10 md:col-span-7"
  >
    <div class="font-bold text-sm sm:text-base md:text-lg mb-1 leading-snug">{title}</div>
    <p class="text-gray-400 sm:text-xs md:text-sm mb-2">
      {subtitle}
      <span class="text-gray-500"> · {type}</span>
    </p>
    <TextContent content={briefDescription} />
    {
      visibleTags.map((tag) => (
        <span class="inline-block bg-gray-300 rounded-full sm:px-2 sm:py-1 md:px-3 md:py-1 sm:mr-1 sm:my-1 md:mr-2 md:my-2 sm:text-xs md:text-sm font-semibold text-gray-700">
          {tag}
        </span>
      ))
    }
  </div>
</CardLink>
